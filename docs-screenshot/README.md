# docs-screenshot

ภาพหน้าจอ Firebase Console ที่โจทย์การบ้าน Week 6 กำหนด (แยกจาก `docs/` ซึ่งเป็น Obsidian vault
ของ requirement/design ที่มีโครงสร้างตายตัวอยู่แล้ว — ดู `CLAUDE.md`)

ใส่ภาพอย่างน้อย:
- Firestore Database → **Data** ที่เห็น collection `fundSources`, `projects`, `receipts` พร้อมข้อมูลจริง
- Firestore Database → **Rules** ที่เห็น rule ที่ตั้งไว้ (`firestore.rules`)
