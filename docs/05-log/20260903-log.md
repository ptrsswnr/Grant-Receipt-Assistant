# Log — 2026-09-03

## 22:11 — แก้ไขโมเดลข้อมูล Fund/Project/Researcher ในสเปคหลัก (แก้ปัญหา entity ปนความหมายที่ผู้ใช้ชี้)

**บริบท**: ผู้ใช้ (เจ้าของโปรเจกต์) ชี้ประเด็นว่าโมเดลข้อมูลที่เขียนไว้ใน
[[20260816-01-grant-receipt-verification]] (และ `docs/02-design/02-technical/db-spec.md`) ผิด —
entity "Fund" (ทุนวิจัย) เดิมเขียนว่ามี `ownerUserId` ผูกตรงกับนักวิจัยคนเดียว ซึ่งปนแนวคิด "แหล่งทุน"
(fund source — มีรหัส/ระเบียบใบเสร็จของตัวเอง ให้ทุนได้หลายโครงการ) เข้ากับ "โครงการวิจัย" (project —
สิ่งที่นักวิจัยเป็นเจ้าของจริง และเป็นสิ่งที่ใบเสร็จควรผูกด้วย)

**เอกสารที่แก้ไข**:
- [[20260816-01-grant-receipt-verification]] — เพิ่มหัวข้อ 11 (ส่วนแก้ไขจากการชี้ประเด็นของผู้ใช้
  2026-09-03) พร้อมแก้ไขหัวข้อ 2.1 (ขอบเขต), หัวข้อ 3 (บทบาทผู้ใช้), และตาราง FR ในหัวข้อ 4
- [[backlog]] — อัปเดตคำอธิบาย FR ที่แก้ไข, เพิ่มแถว FR-23, เพิ่ม Open Point ใหม่สำหรับ 2 ประเด็นที่
  รอการยืนยันจากผู้ใช้

**รหัส FR ที่เพิ่ม/แก้ไข**:
- เพิ่มใหม่: **FR-23** — จัดการข้อมูลโครงการวิจัยและผูกกับแหล่งทุน (Project Setup)
- แก้ไขคำอธิบาย (ไม่เปลี่ยนรหัส): **FR-01** (ใบเสร็จผูกกับโครงการ ไม่ใช่แหล่งทุน), **FR-04** (Rule
  Engine อ้างอิงระเบียบของแหล่งทุนที่โครงการสังกัด), **FR-08** (Dashboard สรุปตามโครงการ), **FR-11**
  (Admin จัดการระเบียบต่อแหล่งทุนแต่ละแหล่ง), **FR-12** (สิทธิ์เข้าถึงอิงตามความเป็นเจ้าของโครงการ)
- ไม่มีการเพิ่ม/แก้ไข NFR ในรอบนี้

**เหตุผลการตัดสินใจ (แก้เอกสารเดิม ไม่แยกเอกสารใหม่/ไม่แทนที่ทั้งฉบับ)**: เป็นการแก้ไขนิยาม entity ที่
กระทบ FR ซึ่งมีอยู่แล้วในเอกสารนี้โดยตรง (FR-01/FR-04/FR-08/FR-11/FR-12) ไม่ใช่หัวข้อใหม่พอที่ควรแยก
และไม่ใช่การเปลี่ยนทิศทางที่ทำให้เอกสารเดิมล้าสมัยทั้งฉบับ (ขอบเขต/บทบาทหลัก/FR ส่วนใหญ่ยังใช้ได้)
สอดคล้องกับ precedent ที่เอกสารนี้เคยใช้มาก่อนกับหัวข้อ 9/10 (เพิ่ม FR-19/FR-21/FR-22 แบบ addendum
ต่อท้าย) — ตัดสินใจโดยผู้ใช้เลือกทางเลือก "ปรับเต็มรูปแบบ" จาก 3 ทางเลือกที่เสนอ (ดูหัวข้อ 11.0 ของ
[[20260816-01-grant-receipt-verification]])

**คำถามที่ควรถามผู้ใช้แต่ยังไม่ได้ถาม (ข้อจำกัดของ agent รอบนี้: ไม่มีเครื่องมือถามผู้ใช้แบบ
interactive ในบริบทที่ทำงานอยู่)** — บันทึกไว้ในหัวข้อ 11.1 ของสเปคหลักและ Open Point ใหม่ใน backlog
พร้อมทางเลือกและคำแนะนำ แต่ **ยังไม่ finalize จนกว่าผู้ใช้จะยืนยัน**:

| # | ประเด็น | ทางที่แนะนำ (ยังไม่ finalize) |
|---|---------|-------------------------------|
| 1 | 1 โครงการมีนักวิจัยเจ้าของได้กี่คน | 1 คนต่อโครงการ (ตรงกับตัวอย่าง `researcher01 > project` ที่ผู้ใช้ยกมาเอง) |
| 2 | Rule Engine ควรอ้างอิงระเบียบของแหล่งทุนเวอร์ชันใดเมื่อระเบียบเปลี่ยนระหว่างอายุโครงการ | เวอร์ชันที่ active ณ วันที่ตรวจใบเสร็จ (สอดคล้อง NFR-04 Audit Trail) |

**สถานะการอัปเดต backlog.md**: อัปเดตแล้ว — เพิ่มแถว FR-23 ในตารางระดับความสำคัญสูง, ปรับคำอธิบาย
FR-01/FR-04/FR-08/FR-11/FR-12, เพิ่ม note อัปเดตที่หัวเอกสาร, เพิ่ม Open Point section ใหม่สำหรับ 2
ประเด็นที่รอยืนยัน

**งานที่ต้องส่งต่อ (ยังไม่ทำในรอบนี้)**: `sync-technical-spec` ต้องปรับ `db-spec.md`/`architecture.md`/
`api-spec.md` ให้แยก entity FundSource/Project/Researcher และย้าย relation `Receipt → Fund` เป็น
`Receipt → Project` — ควรรอคำตอบของ 2 ประเด็นข้างต้นก่อนออกแบบ schema โดยละเอียด

## 22:17 — Finalize 2 ประเด็นที่ค้างไว้จากรอบก่อนหน้า (โมเดลข้อมูล Fund/Project/Researcher)

**บริบท**: coordinator แจ้งว่าได้ถามผู้ใช้แทน (ผ่าน AskUserQuestion) สำหรับ 2 ประเด็นที่บันทึกไว้เป็น
open point ในรอบ 22:11 และได้คำตอบยืนยันแล้ว:

1. 1 โครงการวิจัยมีนักวิจัยเจ้าของโครงการได้กี่คน → **1 คนต่อโครงการ** (ตามที่แนะนำ)
2. Rule Engine ควรใช้ระเบียบของแหล่งทุนเวอร์ชันใดเมื่อตรวจใบเสร็จ → **เวอร์ชันที่ active ณ วันตรวจ**
   (ตามที่แนะนำ)

**เอกสารที่แก้ไข**:
- [[20260816-01-grant-receipt-verification]] — แก้หัวข้อ 11.1 จาก "ประเด็นที่ต้องรอการยืนยัน" เป็น
  "ประเด็นที่ถามผู้ใช้เพิ่มเติมและคำตอบที่ยืนยันแล้ว (finalize 2026-09-03)" ตัดคำว่า "เสนอ/ยังไม่
  finalize" ออก, ปรับคำอธิบาย **FR-04** (ระบุเวอร์ชันระเบียบที่ใช้ตรวจชัดเจน แทนที่ข้อความ "รอการ
  ยืนยัน"), ปรับคำอธิบาย **FR-23** (ระบุจำนวนเจ้าของต่อโครงการ = 1 คน แทนที่ข้อความ "รอการยืนยัน"),
  ปรับหัวข้อ 11.3 ให้ระบุว่าคำตอบทั้งสองข้อ finalize แล้ว ไม่ต้องรอก่อนออกแบบ schema
- [[backlog]] — เพิ่ม note อัปเดตรอบ 2 ที่หัวเอกสาร, ปิด Open Point ที่เกี่ยวข้องโดยระบุว่า finalize
  แล้วทั้ง 2 ประเด็น (เปลี่ยนหัวข้อจาก "ต้องยืนยันก่อนออกแบบ schema" เป็น "ปิดแล้ว, ต้อง sync ก่อน
  design ถัดไป")

**รหัส FR ที่กระทบ**: ไม่มีรหัสใหม่ — เป็นการ finalize รายละเอียดของ **FR-04** และ **FR-23** ที่เพิ่ม/
แก้ไขไปแล้วในรอบ 22:11 ให้สมบูรณ์ ไม่ใช่ข้อเสนอที่รอยืนยันอีกต่อไป

**สรุปคำถาม-คำตอบ**: ดูตารางในหัวข้อบริบทด้านบน (คำตอบตรงกับทางเลือกที่แนะนำไว้ทั้ง 2 ข้อ)

**เหตุผลการตัดสินใจ**: ไม่มีการสร้าง/แยกเอกสารใหม่ในรอบนี้ — แก้ไข section ที่เพิ่งเพิ่มไปแล้วในเอกสาร
เดิมให้สอดคล้องกับคำตอบที่ได้รับ ตรงไปตรงมา

**สถานะการอัปเดต backlog.md**: อัปเดตแล้ว — Open Point ที่เกี่ยวข้องปิดสถานะเป็น finalize แล้วทั้งคู่

**งานที่ต้องส่งต่อ (ยังไม่ทำในรอบนี้ ตามที่ coordinator แจ้งว่าจะสั่งแยกเอง)**: `sync-technical-spec`
เพื่อปรับ `db-spec.md`/`architecture.md`/`api-spec.md` ให้ตรงกับ FundSource/Project/Researcher และ
คำตอบทั้ง 2 ข้อที่ finalize แล้ว

## 22:30 — Sync feature-list.md/user-journey.md กับ backlog/spec ที่แก้ไขโมเดลข้อมูล Fund/Project/Researcher (feature-journey-writer)

**บริบท**: `architecture-writer` ตรวจแล้วยืนยันว่า `feature-list.md`/`user-journey.md` ไม่สอดคล้อง
กับ backlog/spec เวอร์ชันล่าสุด (ยังใช้คำว่า "ทุนวิจัย"/"เจ้าของทุน" ทั้งไฟล์ และไม่มี **FR-23** ใหม่)
จึงส่งงานมาให้ sync ก่อนที่จะดำเนินการ `sync-technical-spec` ต่อ

**สถานะก่อนเริ่มงาน**: **ไม่ up to date** — ทั้งสองไฟล์ยังใช้คำว่า "นักวิจัย/เจ้าของทุน" ทั้งไฟล์ (ควร
เป็น "นักวิจัย/เจ้าของโครงการ" ตามหัวข้อ 3 ของสเปคที่แก้ไขแล้ว), ฟีเจอร์ที่อ้างอิง FR-01/FR-04/FR-08/
FR-11/FR-12 ยังใช้คำอธิบายเดิมที่ปนแนวคิด "แหล่งทุน" กับ "โครงการ", และ **FR-23 (Project Setup)
ยังไม่มีฟีเจอร์ใดๆ ครอบคลุมเลยใน feature-list.md** (ถือเป็นรหัส "ขาดหาย" ทั้งหมด) — mermaid diagram
เดิมของ Journey 1 ตรงกับข้อความอยู่แล้วก่อนแก้ไข (ไม่ใช่ปัญหาความไม่ตรงกันระหว่าง diagram/ข้อความ)
และตารางสรุป MoSCoW ของ feature-list.md มีอยู่แล้วครบ/ตรงกับ heading ก่อนหน้านี้ (ปัญหาคือเนื้อหา
ยังไม่ sync กับ backlog ไม่ใช่ปัญหาโครงสร้างตาราง)

**การตัดสินใจเรื่องตำแหน่งฟีเจอร์ใหม่ (FR-23)**: agent รอบนี้ไม่มีเครื่องมือ `AskUserQuestion` ในบริบท
ที่ทำงานอยู่ (เป็น subagent) จึงตัดสินใจเองแทนการถามผู้ใช้ โดยเลือก **เพิ่ม FR-23 เป็นฟีเจอร์ที่ 13
ต่อท้ายสุด** (ไม่ renumber ฟีเจอร์ 1-12 เดิม) แทนที่จะแทรกเป็นฟีเจอร์ที่ 1 แล้ว renumber ทั้งหมด
เหตุผล: ไฟล์ `docs/02-design/02-technical/detailed-design/*.md`, `docs/03-testing/01-test-plan/
test-cases/*.md`, `architecture.md`, `api-spec.md`, `db-spec.md` อ้างอิงกลับมาที่
`[[feature-list#N. ชื่อฟีเจอร์|ฟีเจอร์ที่ N]]` ด้วยเลขเดิมอยู่แล้วจำนวนมาก การ renumber จะทำให้ wikilink
เหล่านั้นพังทันทีทั้งหมด ในขณะที่การต่อท้ายเป็นฟีเจอร์ที่ 13 ไม่กระทบ wikilink เดิมเลย — **จุดนี้ควร
ให้ผู้ใช้ยืนยันอีกครั้งในเทิร์นถัดไปหากต้องการลำดับที่สะท้อน workflow จริง (ตั้งค่าโครงการก่อนอัปโหลด)**

**ไฟล์ที่แก้ไข**:
- `docs/02-design/feature-list.md`:
  - เปลี่ยนคำว่า "นักวิจัย/เจ้าของทุน" → "นักวิจัย/เจ้าของโครงการ" ทั้งไฟล์ (บรรทัด intro + ทุกบรรทัด
    "บทบาทผู้ใช้:" ของฟีเจอร์ 1-12)
  - เพิ่ม callout อัปเดต 2026-09-03 อธิบายการเปลี่ยนแปลงที่ต้นไฟล์
  - อัปเดตตารางสรุปฟีเจอร์: แถว 1 (เพิ่ม "ผูกกับโครงการวิจัยของตนเอง"), แถว 3 (อ้างอิงระเบียบแหล่งทุน/
    เวอร์ชัน active), แถว 5 (ชื่อฟีเจอร์ + คำอธิบาย "ของโครงการ"), แถว 8 (ชื่อฟีเจอร์ + คำอธิบาย
    "ต่อแหล่งทุนแต่ละแหล่ง"), แถว 9 (คำอธิบาย "โครงการที่ตนเองเป็นเจ้าของ"), เพิ่มแถวใหม่ **แถว 13**
    (Project Setup, Must have, FR-23)
  - อัปเดตหัวข้อรายละเอียด+MoSCoW ของฟีเจอร์ที่ 1, 3, 5 (เปลี่ยนชื่อ heading), 8 (เปลี่ยนชื่อ heading),
    9 ให้ตรงกับตารางสรุป และอัปเดตป้ายกำกับ FR-01/FR-04/FR-08/FR-11/FR-12 ใน bullet list ให้ตรงกับ
    ชื่อหัวข้อที่แก้ไขแล้วใน backlog
  - เพิ่มหัวข้อใหม่ทั้งหมด **## 13. จัดการข้อมูลโครงการวิจัยและผูกกับแหล่งทุน (Project Setup)** พร้อม
    คำอธิบาย, รหัส FR-23, บทบาทผู้ใช้, MoSCoW = Must have (map จากระดับ "สูง" ใน backlog)
  - อัปเดตบรรทัดอ้างอิงเอกสารท้ายไฟล์ให้รวม FR-23
- `docs/02-design/user-journey.md`:
  - เปลี่ยนคำว่า "นักวิจัย/เจ้าของทุน" → "นักวิจัย/เจ้าของโครงการ" ทั้งไฟล์ (ทุกบรรทัด "บทบาท:")
  - เพิ่ม callout อัปเดต 2026-09-03 ที่ต้นไฟล์
  - **Journey 1**: แทรก step ใหม่ (step 4, decision node) "ตรวจสอบว่ามีโครงการวิจัยที่ผูกกับแหล่งทุน
    แล้วหรือยัง (FR-23) → ถ้ายังไม่มี ให้สร้าง/จัดการโครงการและผูกกับแหล่งทุนก่อน" เข้าไปก่อนขั้นตอน
    อัปโหลดใบเสร็จเดิม (ตัดสินใจแทรกใน journey เดิมแทนสร้างใหม่ เพราะเป็นขั้นตอนต่อยอด/ก่อนหน้าของ
    flow เดิม ไม่ใช่สถานการณ์ใหม่ที่ต้องแยก journey — ไม่มีเครื่องมือถามผู้ใช้ในบริบทนี้ จึงตัดสินใจ
    ตามหลักการที่ระบุไว้ในกฎข้อ 6 โดยตรง ไม่ถือเป็นจุดกำกวมที่ต้องถาม) — อัปเดต mermaid diagram
    (เพิ่ม node P, Q) ให้ตรงกับข้อความ, renumber ขั้นตอนข้อความที่ 4-10 เดิมเป็น 5-11, ปรับ node K
    ให้ระบุอ้างอิงระเบียบแหล่งทุน/เวอร์ชัน active
  - **Journey 2**: เปลี่ยนชื่อ heading และคำใน mermaid/ข้อความจาก "ทุน" → "โครงการ" ให้ตรงกับ FR-08
    ที่แก้ไขแล้ว, แก้ reference "Journey 1 ขั้นตอนที่ 10" → "11" ตามการ renumber
  - **Journey 3**: แก้ reference "Journey 1 ขั้นตอนที่ 10" → "11" เท่านั้น (คงคำว่า "ทุน" ไว้ตามเดิม
    เพราะ FR-10/FR-21 ที่ journey นี้อ้างอิงยังไม่ถูกแก้ไขคำศัพท์ในสเปค/backlog รอบนี้ — ไม่ใช่
    ความไม่สอดคล้องที่ต้องแก้ในรอบนี้ แต่เป็นความไม่ลงรอยกันที่มีอยู่แล้วในสเปคเอง)
  - **Journey 4**: เปลี่ยนชื่อ heading และคำใน mermaid/ข้อความให้สะท้อนว่า Admin จัดการระเบียบ
    "ต่อแหล่งทุนแต่ละแหล่ง" (ไม่ใช่ระเบียบเดียวรวมมหาวิทยาลัย) ตรงกับ FR-11 ที่แก้ไขแล้ว, แก้ reference
    "Journey 1 ขั้นตอนที่ 8" → "9"
  - Journey 5-7 ไม่มีการแก้ไข (ไม่ได้อ้างอิงคำว่า "ทุน"/FR ที่ถูกแก้ไขในรอบนี้)
  - อัปเดตบรรทัดอ้างอิงเอกสารท้ายไฟล์ให้รวม FR-23

**ผลกระทบที่ทราบแต่ไม่ได้แก้ (นอกขอบเขตงานนี้)**: การเปลี่ยนชื่อ heading ของฟีเจอร์ที่ 5/8 ใน
feature-list.md และชื่อ Journey 2/4 ใน user-journey.md ทำให้ wikilink แบบ
`[[feature-list#5. Dashboard สรุปภาพรวมใบเสร็จของทุน|...]]` และ
`[[user-journey#Journey 4: Admin นำเข้า/อัปเดตระเบียบทุนวิจัยเข้าสู่ Rule Engine|...]]` ที่มีอยู่แล้วใน
`docs/02-design/02-technical/detailed-design/fund-dashboard.md`, `admin-rule-management.md` และไฟล์
test-case ที่เกี่ยวข้อง **ใช้ไม่ได้ทันที** (anchor text เปลี่ยน) — เป็นผลข้างเคียงที่คาดไว้แล้วและต้อง
แก้ต่อผ่าน `sync-technical-spec`/`sync-test-plan` รอบถัดไป ไม่ได้แก้ในงานนี้เพราะอยู่นอกขอบเขตไฟล์ที่
อนุญาตให้แก้ (`feature-list.md`, `user-journey.md`, log เท่านั้น)

**คำถามที่ควรถามผู้ใช้แต่ยังไม่ได้ถาม (ข้อจำกัดเดียวกับรอบ 22:11 — ไม่มี `AskUserQuestion` ใน context
ของ subagent นี้)**:

| # | ประเด็น | ทางที่เลือกไปก่อน (ยังไม่ยืนยันจากผู้ใช้) |
|---|---------|----------------------------------------|
| 1 | ตำแหน่งฟีเจอร์ FR-23 ในตารางสรุป/heading | เพิ่มเป็นฟีเจอร์ที่ 13 ต่อท้ายสุด (ไม่ renumber) — ทางเลือกอื่นคือแทรกเป็นฟีเจอร์ที่ 1 แล้ว renumber ทั้งหมดให้ตรง workflow จริง |

**สถานะการอัปเดต feature-list.md/user-journey.md**: อัปเดตแล้วทั้งสองไฟล์ให้ตรงกับ backlog/spec
เวอร์ชันล่าสุด (รวม FR-23) — mermaid diagram ของ Journey 1/2/4 ที่แก้ไขตรงกับรายการขั้นตอนข้อความ
100% ตามที่ตรวจสอบแล้ว, ตารางสรุป MoSCoW ตรงกับบรรทัด MoSCoW ในหัวข้อรายละเอียดของทุกฟีเจอร์
รวมฟีเจอร์ที่ 13 ใหม่

**งานที่ต้องส่งต่อ**: `sync-technical-spec` (ตามที่ระบุไว้แล้วในรอบก่อนหน้า) และควรพิจารณา
`sync-test-plan` ในลำดับถัดไปเพื่อแก้ wikilink ที่ชี้กลับมาที่ heading เดิมที่เปลี่ยนชื่อไปแล้ว

## (ต่อ) — Sync architecture.md กับ feature-list.md/user-journey.md เวอร์ชันล่าสุด (architecture-writer)

**บริบท**: coordinator แจ้งว่า `feature-journey-writer` เพิ่ง sync `feature-list.md`/`user-journey.md`
เสร็จแล้ว (รายการด้านบน) รองรับโมเดลข้อมูลใหม่ FundSource/Project/Researcher + ฟีเจอร์ที่ 13 (FR-23,
Project Setup) — gate เดิมที่เคยบล็อกไม่ให้ทำงานต่อถูกปลดแล้ว ให้ตรวจสอบ/ปรับปรุง
`docs/02-design/02-technical/architecture.md` ต่อให้ตรงกัน

**สถานะก่อนเริ่มงาน**: **ไม่ up to date** — `architecture.md` (เขียนล่าสุดเมื่อ 2026-08-23 ก่อนการแก้
โมเดลข้อมูล) ยังใช้แนวคิด "ทุน" (สิ่งที่นักวิจัยเป็นเจ้าของโดยตรง) เป็นก้อนเดียว ทั้ง Actor label
("นักวิจัย/เจ้าของทุน"), Component Diagram (หัวข้อ 2 — Primary Data Store บรรยายว่าเก็บ "ผู้ใช้/ทุนที่
ถือ" ไม่แยก entity), ตารางขอบเขตความรับผิดชอบ (2.1), Data Flow Diagram ทุก journey (หัวข้อ 3, actor
label เดิมทั้งหมด), และไม่มี FR-23/ฟีเจอร์ที่ 13 (Project Setup) ปรากฏอยู่เลย (ถือเป็น "ขาดหาย" ทั้งหมด)
— ตรวจสอบแล้วว่า `technology-stack.md` ยังไม่มีไฟล์อยู่จริงในโปรเจกต์ (อ่านแล้วได้ error "File does not
exist") จึงยังคงเขียนเอกสารนี้แบบ logical/conceptual ล้วนต่อไปตามเดิม ไม่ผูก tech stack ใดๆ

**เทียบกับ feature-list.md/user-journey.md เวอร์ชันล่าสุด**: ตรวจสอบ FR ทั้งหมดที่ระบุใน backlog
(FR-01–FR-23) เทียบกับ architecture.md แล้วพบว่า FR-01–FR-22 มี component ที่ map อยู่แล้วครบ (แค่ต้อง
แก้คำศัพท์ให้ตรงโมเดลใหม่) มีเพียง **FR-23 เท่านั้นที่ขาดหายทั้งหมด** — ไม่พบ NFR ใดที่ยังไม่ถูก map
(NFR-01–NFR-11 ครอบคลุมอยู่แล้วในหัวข้อ 4 เดิม การแก้ไขรอบนี้เป็นการปรับคำอธิบายให้สะท้อน FundSource/
Project แทนที่ "ทุน" เฉยๆ ไม่ใช่การเพิ่ม mapping ใหม่)

**ไฟล์ที่แก้ไข**: `docs/02-design/02-technical/architecture.md`
- หัวข้อ 0 (สถานะเอกสาร): เพิ่ม bullet อัปเดต 2026-09-03 สรุปการ sync รอบนี้และเหตุผลที่ไม่พบจุดต้องถาม
  ผู้ใช้เพิ่มเติม (โมเดลข้อมูล finalize แล้วทั้งหมดใน backlog ก่อนเริ่มงาน)
- หัวข้อ 1 (ภาพรวม): เปลี่ยน "นักวิจัย/เจ้าของทุน" → "นักวิจัย/เจ้าของโครงการ", เพิ่ม bullet ใหม่อธิบาย
  การแยก 3 concept (FundSource/Project/Researcher) และผลกระทบต่อโครงสร้าง component โดยตรง
- หัวข้อ 2 (Component Diagram, mermaid): แก้ Actor label, เพิ่ม "Project/FundSource Setup (FR-23)"
  เข้า Backend Service box, เพิ่มคำอธิบาย "อ้างอิงระเบียบของ FundSource ที่ Project สังกัด" เข้า Rule
  Engine box, แก้ Primary Data Store box ให้ระบุ Researcher/FundSource(1:N Project)/Project(1 เจ้าของ)/
  ใบเสร็จผูกกับ Project แทนคำว่า "ผู้ใช้/ทุนที่ถือ" เดิม
- หัวข้อ 2.1 (ตารางขอบเขตความรับผิดชอบ): แก้ไขคำอธิบายของ Client, Backend Service, Verification Rule
  Engine, Primary Data Store ให้ระบุ FundSource/Project/Researcher แยกจากกันชัดเจน + เพิ่ม FR-23 เข้า
  คอลัมน์ FR/NFR ที่เกี่ยวข้องของ Client/Backend Service/Primary Data Store — ไม่ต้องเพิ่ม component
  ใหม่ เพราะ Project Setup (FR-23) เป็นแค่ CRUD ผ่าน Backend Service/Primary Data Store ที่มีอยู่แล้ว
- หัวข้อ 3 (ย่อหน้าเปิด): เพิ่มหมายเหตุว่าฟีเจอร์ที่ 13/FR-23 ไม่มี journey แยก (ผูกเข้า Journey 1 ตามที่
  `feature-journey-writer` ตัดสินใจไว้แล้ว) จึงสะท้อนเป็น gate เพิ่มเติมใน sequence diagram แทน
- หัวข้อ 3.1 (Journey 1): เพิ่ม gate ใหม่ "ตรวจสอบ Project ที่ผูกกับ FundSource แล้วหรือยัง (FR-23)"
  ต่อจาก consent gate เดิม ก่อนขั้นตอนอัปโหลดไฟล์ (มี alt block สร้าง Project ใหม่/เลือก Project เดิม)
  ตรงกับ node P/Q ใน mermaid flowchart ของ user-journey.md Journey 1, แก้ actor label, แก้ข้อความ
  อัปโหลด/ผูกใบเสร็จให้ระบุ "ผูกกับ Project" แทน "ผูกกับทุน", แก้ข้อความเรียก Rule Engine ให้ระบุการส่ง
  FundSource ที่ Project สังกัดไปด้วย
- หัวข้อ 3.2–3.6 (Journey 2-7 diagrams): แก้ actor label เป็น "นักวิจัย/เจ้าของโครงการ" ทุก diagram,
  แก้คำว่า "ทุน" เป็น "โครงการวิจัย (Project)" ใน Journey 2/3 (Dashboard/Export ผูกกับ Project), แก้
  Journey 4 (Admin) ให้ระบุ "แหล่งทุน (FundSource)" แทน "ทุนวิจัย" ให้ตรงกับ FR-11 ที่แก้ไขแล้ว —
  Journey 5-7 (consent/erasure) แก้เฉพาะ actor label เพราะเนื้อหาไม่เกี่ยวกับ FundSource/Project
- หัวข้อ 4 (NFR Mapping): ปรับคำอธิบายของ NFR-04 (immutable ผูกกับเวอร์ชันระเบียบของ FundSource ที่
  Project สังกัด ณ ขณะตรวจ แม้ Project จะย้าย FundSource ภายหลังก็ไม่กระทบผลตรวจเก่า), NFR-05
  (สิทธิ์อิงตามเจ้าของ Project), NFR-07 (Scalability ต้องรองรับ Researcher/Project/FundSource เพิ่มขึ้น
  ตามจำนวนโครงการ ไม่ใช่แค่จำนวนแหล่งทุน)
- ท้ายเอกสาร ("เอกสารที่เกี่ยวข้อง"): อัปเดตช่วง FR เป็น FR-01–FR-23, จำนวนฟีเจอร์เป็น 13, หมายเหตุว่า
  FR-23 ผูกเข้า Journey 1 ไม่ใช่ journey แยก, วันที่ตรวจสอบ technology-stack.md เป็น 2026-09-03

**จุดที่ตรวจสอบว่าไม่มีความกำกวมต้องถามผู้ใช้ (ตามมาตรฐาน AskUserQuestion ของ CLAUDE.md)**: พิจารณาแล้ว
ว่าโมเดลข้อมูล FundSource/Project/Researcher, ความสัมพันธ์ 1:N/1:1 ทั้งหมด, และเวอร์ชันระเบียบที่ Rule
Engine อ้างอิง **finalize แล้วทั้งหมดใน [[backlog]]** ก่อนเริ่มงานนี้ (ดูหัวข้อ 11.1 ของ
[[20260816-01-grant-receipt-verification]]) ไม่มีทางเลือกออกแบบระดับ component ใหม่ที่ยังกำกวมเกิดขึ้น
จากการเปลี่ยนแปลงนี้ (Project Setup เป็นแค่ CRUD ผ่าน component เดิมที่มีอยู่แล้ว ไม่ต้องเพิ่ม component
ใหม่หรือเลือกรูปแบบการสื่อสารใหม่) จึงไม่ได้เรียก `AskUserQuestion` ในรอบนี้

**สถานะการอัปเดต architecture.md**: อัปเดตแล้ว ครอบคลุม FR-01–FR-23/NFR-01–NFR-11 ครบถ้วน สอดคล้องกับ
`feature-list.md` (13 ฟีเจอร์) และ `user-journey.md` (7 journey, FR-23 ผูกเข้า Journey 1) เวอร์ชัน
ล่าสุดแล้ว — ไม่มีเนื้อหาที่ถูกลบทิ้งโดยไม่จำเป็น (ทุกการแก้ไขเป็นการเพิ่ม/ปรับคำอธิบายให้ตรงโมเดลใหม่)

**งานที่ต้องส่งต่อ**: `sync-technical-spec` ส่วนที่เหลือ (`api-spec.md`, `db-spec.md`,
`detailed-design/`, `nfr-review.md`) ยังต้องปรับให้ตรงกับโมเดล FundSource/Project/Researcher และ
FR-23 เช่นกัน — งานนี้ครอบคลุมเฉพาะ `architecture.md` ตามขอบเขตที่ได้รับมอบหมายเท่านั้น

## (ต่อ) — Sync api-spec.md/db-spec.md กับ architecture.md/feature-list.md เวอร์ชันล่าสุด (api-db-writer)

**บริบท**: `architecture.md` เพิ่ง sync เสร็จ (รายการด้านบน) รองรับโมเดล FundSource/Project/
Researcher + FR-23 (Project Setup, ฟีเจอร์ที่ 13) — ให้ตรวจสอบ/ปรับปรุง
`docs/02-design/02-technical/api-spec.md` และ `docs/02-design/02-technical/db-spec.md` ต่อให้ตรงกัน
(ต้องแก้คู่กันเสมอเพราะ field ของ operation ต้องตรงกับ attribute ของ entity)

**สถานะก่อนเริ่มงาน**: **ไม่ up to date ทั้งสองไฟล์** — เขียนล่าสุดเมื่อ 2026-08-23 (รอบ FR-22) ก่อน
การแก้โมเดลข้อมูล ยังใช้ entity/field เดิมที่ผิด: `db-spec.md` มี entity "Fund" (E-02) ที่มี
`ownerUserId` ผูกตรงกับนักวิจัย 1 คน, `Receipt.fundId` ชี้ตรงไปยัง Fund, `RuleVersion` ไม่มี
`fundSourceId` (สื่อว่าระเบียบเดียวรวมทั้งระบบ), `ExportBatch.fundId`/`AuditLogEntry.affectedFundId`
ผูกกับ Fund เดิม — `api-spec.md` มี operation หลายตัว (OP-01, OP-04, OP-08, OP-10, OP-11, OP-12,
OP-15) ใช้ `fundId`/แนวคิด "ระเบียบเดียวรวมทั้งระบบ" ตรงกัน และ**ไม่มี operation ใดรองรับ FR-23
(Project Setup) เลย** — ถือเป็นช่องว่างสำคัญ (ขาดหายทั้งหมดสำหรับ FR-23)

**เทียบกับ architecture.md/feature-list.md เวอร์ชันล่าสุด**: FR-01–FR-22, NFR-01–NFR-11 มี
operation/entity รองรับอยู่แล้วครบ (ต้องแก้ field/ความสัมพันธ์ให้ตรงโมเดลใหม่เท่านั้น) มีเพียง
**FR-23 เท่านั้นที่ไม่มี operation ใดรองรับเลย** — ไม่พบ FR/NFR อื่นที่ยังขาดหาย

**ไฟล์ที่แก้ไข**:
- `docs/02-design/02-technical/db-spec.md`:
  - หัวข้อ 0: เพิ่ม bullet อัปเดต 2026-09-03 สรุปการแก้ไขและเหตุผลที่ไม่พบจุดต้องถามผู้ใช้เพิ่มเติม
  - หัวข้อ 1 (ER Diagram, mermaid): แทนที่ `FUND` ด้วย `FUNDSOURCE` (ตัด `ownerUserId` ออก) และเพิ่ม
    entity ใหม่ `PROJECT` (มี `ownerUserId`, `fundSourceId`) เข้าไปแทนตำแหน่งเดิม, เปลี่ยน
    `RECEIPT.fundId` → `RECEIPT.projectId`, เพิ่ม `RULEVERSION.fundSourceId`, เปลี่ยน
    `EXPORTBATCH.fundId` → `EXPORTBATCH.projectId`, ปรับความสัมพันธ์ทั้งหมดให้ตรง
    (`FUNDSOURCE ||--o{ PROJECT`, `USER ||--o{ PROJECT`, `PROJECT ||--o{ RECEIPT`,
    `FUNDSOURCE ||--o{ RULEVERSION`) พร้อมหมายเหตุ cardinality เพิ่มเติม (1 โครงการ 1 เจ้าของ)
  - หัวข้อ 2 (รายละเอียด Entity): **จัดลำดับรหัส E- ใหม่ทั้งหมด** — E-02 เดิม "Fund" → **E-02
    FundSource** (ตัด `ownerUserId`), เพิ่ม **E-03 Project** ใหม่ (`ownerUserId`, `fundSourceId`),
    E-03 เดิม "Receipt" → **E-04** (`fundId` → `projectId`), E-13 เดิม "ReceiptFile" ย้ายมาเป็น
    **E-05** (ต่อจาก Receipt ทันที), E-04–E-12 เดิมเลื่อนเป็น E-06–E-14 ตามลำดับ (RuleVersion เพิ่ม
    `fundSourceId`, VerificationResult ปรับคำอธิบาย snapshot ให้ผูกกับ FundSource ของ Project,
    ExportBatch เปลี่ยน `fundId`→`projectId`, AuditLogEntry เปลี่ยน `affectedFundId`→
    `affectedProjectId`)
  - หัวข้อ 3 (สรุปความสัมพันธ์): แก้ทุกแถวที่อ้างอิง Fund ให้เป็น FundSource/Project ตามบทบาทจริง
  - หัวข้อ 4 (กฎทางธุรกิจ): แก้กฎข้อ 2 (snapshot ผ่าน FundSource ของ Project), กฎข้อ 6 (ไม่ลบ
    Project/FundSource แทน "ไม่ลบ Fund"), กฎข้อ 7 (RuleVersion.isActive เป็นรายแหล่งทุน ไม่ใช่ทั้ง
    ระบบ) — เพิ่มกฎใหม่ข้อ 11 (การเลือก RuleVersion ต้องมาจาก FundSource ของ Project ณ วันที่ตรวจ)
    และข้อ 12 (Project ownership 1 คนต่อโครงการ)
  - หัวข้อ 5 (ประเด็นรอตัดสินใจ): คงเดิม (5.1–5.4 ยังใช้ได้ ไม่กระทบจากการแก้โมเดลรอบนี้)
- `docs/02-design/02-technical/api-spec.md`:
  - หัวข้อ 0: เพิ่ม bullet อัปเดต 2026-09-03 สรุป field ที่เปลี่ยน (`fundId`→`projectId` ใน OP-01/
    OP-08/OP-10, OP-04/OP-06 เปลี่ยนการเลือก RuleVersion, OP-11/OP-12 ผูกกับ `fundSourceId`, OP-15
    แก้ข้อความอ้างอิง) และ operation ใหม่ที่เพิ่ม (OP-18–OP-21)
  - หัวข้อ 1 (บทบาทผู้เรียก): เปลี่ยน "นักวิจัย/เจ้าของทุน" → "นักวิจัย/เจ้าของโครงการ" ทั้งตาราง
  - หัวข้อ 2 (Cross-cutting): เปลี่ยนกฎจาก `fundId`/`Fund.ownerUserId` เป็น `projectId`/
    `Project.ownerUserId`, เพิ่มข้อ 3 อธิบายว่า `fundSourceId` ไม่มีเจ้าของเฉพาะคน (ไม่ต้องตรวจสอบ
    ownership แบบเดียวกัน)
  - หัวข้อ 3 (Operation Catalog): แก้ FR ที่เกี่ยวข้องของ OP-04/OP-11/OP-12 ให้รวม FR-23, เพิ่มแถวใหม่
    **OP-18 (ดูรายการโครงการวิจัยของตนเอง)**, **OP-19 (สร้างโครงการวิจัยใหม่ผูกกับแหล่งทุน)**,
    **OP-20 (แก้ไขข้อมูลโครงการวิจัย)**, **OP-21 (ดูรายการแหล่งทุนที่มีอยู่)** — ต่อท้ายด้วยเลข
    OP-18–OP-21 (ไม่แทรก/renumber ของเดิม เพื่อไม่กระทบ wikilink ที่ `detailed-design/` อาจอ้างอิง
    OP-01–OP-17 อยู่แล้ว)
  - หัวข้อ 4: เพิ่มหัวข้อใหม่ **4.0 ฟีเจอร์ที่ 13 — Project Setup** (รายละเอียด OP-18–OP-21) ก่อน 4.1
    เพราะตามลำดับ user journey จริงเกิดก่อนอัปโหลดใบเสร็จเสมอ — แก้ **OP-01** (`fundId`→
    `projectId` + error case ใหม่เมื่อยังไม่มี Project), **OP-04** (ลำดับประมวลผลข้อ 2 แก้ให้หา
    FundSource ผ่าน Project ก่อนเลือก RuleVersion + error case ใหม่เมื่อแหล่งทุนไม่มีระเบียบ active),
    **OP-08**/**OP-10** (`fundId`→`projectId`), **OP-09** (คำอธิบาย "โครงการ" แทน "ทุน"), **OP-11**
    (input เพิ่ม `fundSourceId`/`fundSourceName`+`fundSourceCode`, กฎข้อ 1/3 แก้เป็นรายแหล่งทุน),
    **OP-12** (เพิ่ม input `fundSourceId` บังคับ), **OP-15** (แก้ข้อความ "ไม่ลบ Fund" →
    "ไม่ลบ Project/FundSource"), **OP-16** (คำอธิบาย "โครงการวิจัยที่ผู้เรียกเป็นเจ้าของ")
  - หัวข้อ 5 (ประเด็นรอตัดสินใจ): เพิ่มข้อความยืนยันว่าโมเดล FundSource/Project/Researcher finalize
    แล้วตั้งแต่ 2026-09-03 ไม่ใช่ประเด็นเปิดอีกต่อไป
  - ท้ายเอกสารทั้งสองไฟล์ ("เอกสารที่เกี่ยวข้อง"): อัปเดตช่วง FR เป็น FR-01–FR-23, จำนวนฟีเจอร์เป็น 13

**จุดที่ตรวจสอบว่าไม่มีความกำกวมต้องถามผู้ใช้ (ตามมาตรฐาน AskUserQuestion ของ CLAUDE.md)**: พิจารณา
แล้วว่าไม่มีจุดออกแบบที่มีทางเลือกสมเหตุสมผลมากกว่า 1 ทางในรอบนี้:
- โมเดล FundSource/Project/Researcher, cardinality ทั้งหมด (1 โครงการ 1 เจ้าของ, เวอร์ชันระเบียบ
  active ณ วันที่ตรวจ) **finalize แล้วทั้งหมดใน spec หัวข้อ 11.1** ก่อนเริ่มงานนี้ — ไม่ต้องถามซ้ำ
- การออกแบบว่า Admin ควรสร้าง `FundSource` ผ่าน operation แยกต่างหากหรือรวมเข้ากับการนำเข้าระเบียบ
  (OP-11) เป็น implementation detail ที่ตัดสินใจได้ตรงไปตรงมาจากคำอธิบายที่มีอยู่แล้วใน
  architecture/feature-list ("Admin นำเข้าแหล่งทุน...", "รายการแหล่งทุนที่ Admin นำเข้าไว้") — ไม่ใช่
  จุดกำกวมที่มีทางเลือกขัดแย้งกันหลายทางที่ต้องให้ผู้ใช้ตัดสิน (โมเดลเดิมก่อนแก้ไขก็ไม่เคยมี operation
  แยกสำหรับสร้าง Fund เองอยู่แล้วเช่นกัน จึงเลือกแนวทางที่ต่อเนื่องกับพฤติกรรมเดิมของระบบ)
- ตำแหน่งเลข ID ของ operation ใหม่ (OP-18–OP-21 ต่อท้าย แทนที่จะแทรก/renumber) เป็นการตัดสินใจเชิง
  รักษาความเสถียรของ wikilink เดิม ไม่ใช่การตัดสินใจเชิงโดเมน/ธุรกิจที่กระทบผู้ใช้ปลายทาง

**สถานะการอัปเดต api-spec.md/db-spec.md**: อัปเดตแล้วทั้งสองไฟล์ ครอบคลุม FR-01–FR-23/NFR-01–NFR-11
ครบถ้วน สอดคล้องกับ `architecture.md`/`feature-list.md` เวอร์ชันล่าสุด — field ทุกตัวใน operation
ตรงกับ attribute ใน entity ทุกจุด (ตรวจสอบไขว้แล้วทั้ง `projectId`, `fundSourceId`, และ field เดิมที่
ไม่เปลี่ยน)

**งานที่ต้องส่งต่อ**: `sync-technical-spec` ส่วนที่เหลือ (`detailed-design/`, `nfr-review.md`) ยังต้อง
ปรับให้ตรงกับโมเดล FundSource/Project/Researcher, field ใหม่ (`projectId`/`fundSourceId`), และ
operation ใหม่ OP-18–OP-21 — รวมถึงต้องปรับ wikilink ที่ชี้ไปยังรหัส entity เดิม (เช่น
`db-spec#E-02 ทุนวิจัย (Fund)`, `db-spec#E-13 ไฟล์ประกอบใบเสร็จ (ReceiptFile)`) ให้ตรงกับรหัส E- ที่
จัดเรียงใหม่ในรอบนี้ (FundSource เป็น E-02, Project เป็น E-03, Receipt เป็น E-04, ReceiptFile เป็น
E-05 — ดูตารางเทียบเต็มในหัวข้อ 0 ของ `db-spec.md`) — งานนี้ครอบคลุมเฉพาะ `api-spec.md`/`db-spec.md`
ตามขอบเขตที่ได้รับมอบหมายเท่านั้น

## (ต่อ) — Sync detailed-design/ กับ api-spec.md/db-spec.md/feature-list.md เวอร์ชันล่าสุด (detailed-design-writer)

**บริบท**: `api-spec.md`/`db-spec.md` เพิ่ง sync เสร็จ (รายการด้านบน) รองรับโมเดล
FundSource/Project/Researcher, field ใหม่ (`projectId`/`fundSourceId`), รหัส entity ที่จัดเรียงใหม่
(E-02–E-14), และ operation ใหม่ OP-18–OP-21 (FR-23, ฟีเจอร์ที่ 13 — Project Setup) — ให้ตรวจสอบ/
ปรับปรุง `docs/02-design/02-technical/detailed-design/*.md` ทั้งหมดให้ตรงกัน

**สถานะก่อนเริ่มงาน**: **ไม่ up to date ทั้ง 12 ไฟล์ที่มีอยู่ + ขาดหาย 1 ฟีเจอร์ทั้งหมด**
- อ่าน `api-spec.md`, `db-spec.md`, `architecture.md`, `feature-list.md`, `user-journey.md` ทั้งไฟล์
  ก่อนเริ่มงานตามที่กำหนด
- Glob พบไฟล์ที่มีอยู่แล้ว 12 ไฟล์ (ตรงกับฟีเจอร์ 1-12 เดิม) — ทุกไฟล์ยังอ้างอิง `fundId`, entity
  "Fund" (E-02 เดิม), รหัส entity เดิมทั้งหมด (Receipt = E-03 เดิม, ReceiptFile = E-13 เดิม,
  RuleVersion = E-04 เดิม ฯลฯ) และคำเรียกบทบาท "นักวิจัย/เจ้าของทุน" — ล้าสมัยทั้งหมดตามที่คาดไว้ใน
  log ของ `api-db-writer` ด้านบน
- **ฟีเจอร์ที่ 13 (Project Setup, FR-23, OP-18–OP-21) ไม่มีไฟล์ detailed-design เลย** — ถือเป็น
  "ขาดหาย" ทั้งหมด
- ไม่พบไฟล์ที่ล้าสมัยแบบ "อ้างอิงฟีเจอร์ที่ไม่มีอยู่แล้วจริง" (ทุกไฟล์ยังตรงกับฟีเจอร์ 1-12 ที่มีอยู่จริง
  ใน feature-list ปัจจุบัน เพียงแค่เนื้อหาภายในอ้างอิง operation/entity รุ่นเก่า) — จึงไม่มีจุดที่ต้อง
  ใช้ `AskUserQuestion` ถามเรื่อง "ลบทิ้ง/เก็บไว้/ปรับปรุง" เพราะทุกไฟล์เลือกแนวทาง "ปรับปรุงให้ตรงกับ
  operation/entity ปัจจุบัน" ได้ตรงไปตรงมา (ไม่มีทางเลือกขัดแย้งกันหลายทาง)

**ไฟล์ที่แก้ไข (ปรับปรุงเนื้อหาให้ตรงกับโมเดลใหม่ ไม่ลบทิ้ง)**:
- `receipt-upload-ocr.md` (ฟีเจอร์ 1, OP-01/OP-02): `fundId`→`projectId`, cross-cutting ตรวจผ่าน
  `Project.ownerUserId`, เพิ่มเงื่อนไขก่อนหน้า (gate) ต้องมี `Project` ก่อนเสมอ (อ้างอิง
  `[[project-setup]]` ใหม่), แก้รหัส entity (Receipt=E-04, ReceiptFile=E-05)
- `review-edit-ocr-data.md` (ฟีเจอร์ 2, OP-03): แก้ cross-cutting ผ่าน `Project`, รหัส entity
  (Receipt=E-04, Project=E-03)
- `rule-engine-verification.md` (ฟีเจอร์ 3, OP-04/OP-05): แก้ลำดับประมวลผลให้หา `FundSource` ผ่าน
  `Project.fundSourceId` ก่อนเลือก `RuleVersion` ที่ active ณ วันที่ตรวจ (แทน "เวอร์ชันเดียวรวมทั้ง
  ระบบ" เดิม), เพิ่ม error case ใหม่ "ไม่มีระเบียบสำหรับแหล่งทุนนี้", แก้รหัส entity
  (RuleVersion=E-06, RuleItem=E-07, VerificationResult=E-08, VerificationRuleCitation=E-09)
- `resubmit-failed-receipt.md` (ฟีเจอร์ 4, OP-06/OP-07): แก้รหัส entity เท่านั้น (เนื้อหา flow ไม่
  เปลี่ยนจากรอบ FR-23 นอกจากอ้างอิงระเบียบผ่าน Project→FundSource ที่จัดการอยู่ที่
  `rule-engine-verification` แล้ว)
- `fund-dashboard.md` (ฟีเจอร์ 5, OP-08): `fundId`→`projectId`, cross-cutting ผ่าน `Project` — **คง
  ชื่อไฟล์เดิม** (`fund-dashboard.md`) เพื่อให้ตรงกับ slug ที่ test-case ไฟล์เดียวกันใช้อยู่แล้ว (ระบุ
  เหตุผลไว้เป็น callout ในไฟล์)
- `pending-issue-notification.md` (ฟีเจอร์ 6, OP-09): แก้ "ของทุนทั้งหมด" → "ของโครงการทั้งหมด", แก้
  รหัส entity
- `export-verified-report.md` (ฟีเจอร์ 7, OP-10): `fundId`→`projectId`, แก้รหัส entity
  (ExportBatch=E-12, ExportBatchReceipt=E-13)
- `admin-rule-management.md` (ฟีเจอร์ 8, OP-11/OP-12): เพิ่ม `fundSourceId`/สร้าง `FundSource` ใหม่ใน
  การดำเนินการเดียวกันที่ OP-11, เพิ่ม `fundSourceId` เป็น input บังคับของ OP-12, แก้กฎ "1 เวอร์ชัน
  active" จาก "ทั้งระบบ" เป็น "ต่อแหล่งทุนหนึ่ง", แก้รหัส entity (FundSource=E-02, RuleVersion=E-06,
  RuleItem=E-07, AuditLogEntry=E-14)
- `access-control.md` (ฟีเจอร์ 9, cross-cutting): เปลี่ยนกฎจาก `Fund.ownerUserId` เป็น
  `Project.ownerUserId` ทั้งหมด, เพิ่มกฎข้อ 3 (`fundSourceId` ไม่มีเจ้าของเฉพาะคน), เพิ่มแถว
  OP-18–OP-21 ในตารางบังคับใช้กฎ, แก้รหัส entity
- `consent-management.md` (ฟีเจอร์ 10, OP-13/14/15): แก้ "ไม่ลบ Fund" → "ไม่ลบ Project และไม่ลบ
  FundSource", แก้รหัส entity (PrivacyNoticeVersion=E-10, ConsentRecord=E-11, ExportBatchReceipt=E-13,
  AuditLogEntry=E-14)
- `data-access-portability.md` (ฟีเจอร์ 11, OP-16): แก้ "ทุกทุนที่ผู้เรียกถือ" → "ทุกโครงการวิจัยที่
  ผู้เรียกเป็นเจ้าของ", แก้รหัส entity
- `right-to-erasure.md` (ฟีเจอร์ 12, OP-17): แก้รหัส entity เท่านั้น (เนื้อหา flow ไม่เปลี่ยน)
- **`project-setup.md` (ใหม่ทั้งหมด — ฟีเจอร์ 13, OP-18–OP-21)**: สร้างไฟล์ใหม่ครอบคลุม FR-23 ทั้งหมด
  — sequence diagram (OP-18 ดูรายการ Project → OP-21 ดูรายการ FundSource → OP-19 สร้าง Project ใหม่,
  พร้อม opt block ของ OP-20 แก้ไข Project ภายหลัง), ตาราง operation↔entity, หมายเหตุว่าไม่มี state
  diagram ของ `Project` เอง (ไม่มี field สถานะใน db-spec), ตาราง edge case 7 กรณี — **หมายเหตุสำคัญ**:
  ยังไม่พบไฟล์ test case ที่ตรงกันใน `docs/03-testing/01-test-plan/test-cases/` (12 ไฟล์ที่มีอยู่ยัง
  ไม่ครอบคลุมฟีเจอร์ 13) จึงตั้ง slug `project-setup` เองตามรูปแบบเดียวกับไฟล์อื่น และบันทึกไว้ในไฟล์
  ว่าถ้า `test-writer` ตั้งชื่อต่างออกไป ต้องปรับให้ตรงกันในรอบ `sync-technical-spec` ถัดไป

ทุกไฟล์ที่แก้ไขเพิ่มบทที่ "0. สถานะเอกสารนี้" ระบุการเปลี่ยนแปลงรอบ 2026-09-03 ไว้ชัดเจน และเปลี่ยน
actor label ทุก sequence diagram จาก "นักวิจัย/เจ้าของทุน" เป็น "นักวิจัย/เจ้าของโครงการ" ทั้งหมด

**ช่องว่างของ api-spec/db-spec ที่พบระหว่างทาง**: ไม่พบช่องว่างใหม่ — ทุก operation/entity ที่ต้อง
อ้างอิงเพื่อออกแบบทั้ง 13 ฟีเจอร์มีอยู่ครบใน `api-spec.md`/`db-spec.md` เวอร์ชันล่าสุดแล้ว (OP-01–OP-21,
entity E-01–E-14 ทั้งหมด) — ไม่ต้องแนะนำให้รัน `sync-api-db` เพิ่มในรอบนี้

**จุดที่ตรวจสอบว่าไม่มีความกำกวมต้องถามผู้ใช้ (ตามมาตรฐาน AskUserQuestion ของ CLAUDE.md)**: พิจารณาแล้ว
ว่าไม่มีจุดออกแบบ sequence flow/state transition/edge case ใหม่ที่มีทางเลือกสมเหตุสมผลมากกว่า 1 ทางใน
รอบนี้ — ทุก sequence/ลำดับ operation (เช่น OP-18→OP-21→OP-19 ใน Project Setup, ลำดับหา FundSource
ก่อนเลือก RuleVersion ใน OP-04) ถูกกำหนดไว้ชัดเจนแล้วใน `api-spec.md`/`user-journey.md` ที่ finalize
มาก่อนงานนี้ ไม่ต้องตัดสินใจแทนผู้ใช้เพิ่มเติม จึงไม่ได้เรียก `AskUserQuestion` ในรอบนี้

**สถานะการอัปเดต detailed-design/**: อัปเดตแล้วครบทั้ง 13 ไฟล์ (12 ไฟล์เดิม + 1 ไฟล์ใหม่) ตรงกับ
ฟีเจอร์ทั้ง 13 ใน `feature-list.md` ครบถ้วน อ้างอิง operation/entity จริงจาก `api-spec.md`/`db-spec.md`
เวอร์ชันล่าสุดทุกจุด (ไม่มีการคิด operation/entity ใหม่เอง)

**งานที่ต้องส่งต่อ**: แนะนำให้รัน `sync-test-plan` ต่อ เพื่อ (1) สร้างไฟล์ test case สำหรับฟีเจอร์ที่ 13
(Project Setup) ที่ยังไม่มี และ (2) ตรวจสอบว่า test case ไฟล์เดิมทั้ง 12 ไฟล์ยังอ้างอิง `fundId`/
"Fund"/รหัส entity เดิมอยู่หรือไม่ (มีความเป็นไปได้สูงเพราะสร้างพร้อมกับ detailed-design รุ่นเก่า) —
รวมถึงพิจารณา `sync-technical-spec` ต่อยอด `nfr-review.md` ให้สอดคล้องกับโมเดลข้อมูลใหม่ด้วยถ้ายังไม่ทำ

## (ต่อ) — ตรวจสอบ nfr-review.md ซ้ำหลังการแก้ไขโมเดลข้อมูล FundSource/Project/Researcher + FR-23 (nfr-reviewer)

**บริบท**: `detailed-design-writer` เพิ่ง sync ครบทั้ง 13 ไฟล์ (รายการด้านบน) — สั่งให้ตรวจสอบซ้ำว่า
NFR ทุกตัวใน [[backlog]] (NFR-01–NFR-11 ไม่มี NFR ใหม่ในรอบนี้) ยังถูกออกแบบมารองรับจริงหรือไม่ในเอกสาร
เชิงเทคนิคทั้ง 4 ชั้นหลังการแก้ไขโมเดลข้อมูลครั้งใหญ่นี้ โดยเฉพาะ **NFR-03 (Explainability)**,
**NFR-04 (Audit Trail)**, **NFR-05 (Access Control)** — งานนี้เป็นงานตรวจสอบ+รายงานเท่านั้น
ไม่แก้ไข architecture/api-spec/db-spec/detailed-design ใดๆ ทั้งสิ้น

**ขั้นตอนที่ทำ**: อ่าน [[backlog]] ทั้งไฟล์ (grep รหัส NFR- ยืนยัน 11 รายการ NFR-01–NFR-11 เท่าเดิม),
อ่าน [[architecture]], [[api-spec]], [[db-spec]] ทั้งไฟล์, Glob + อ่าน [[access-control]],
[[rule-engine-verification]], [[project-setup]], [[admin-rule-management]] อย่างละเอียด (เจาะจง
NFR-03/04/05) แล้ว grep คำว่า `fundId`/`Fund.ownerUserId`/`[^e]Fund[^S]` ทั่วทั้ง
`docs/02-design/02-technical/` เพื่อยืนยันว่าไม่มีการอ้างอิง entity/field รุ่นเก่าหลงเหลืออยู่จริง

**ผลการตรวจสอบ**: **ไม่พบช่องว่างใหม่จากการแก้ไขโมเดลข้อมูลรอบนี้** — ผลสรุปคงเดิมจากรอบตรวจก่อนหน้า
(2026-08-23): **6 รายการรองรับแล้ว, 2 รายการรองรับบางส่วน (NFR-06, NFR-07 — ไม่เกี่ยวกับการแก้โมเดล
ข้อมูล เป็นช่องว่างเดิมเรื่องตัวเลขเป้าหมายเชิงปริมาณที่ยังไม่กำหนด), 0 รายการยังไม่รองรับเลย**
- **NFR-03**: `api-spec` OP-04 มีลำดับใหม่ชัดเจน (หา `Project.fundSourceId` ก่อนเลือก `RuleVersion`
  ที่ตรงกับ FundSource นั้น) ทำให้ผลตรวจอ้างอิงระเบียบของ FundSource ที่ถูกต้องตามที่โจทย์กังวลจริง —
  `db-spec` E-09 (VerificationRuleCitation) ยังคงบังคับ ≥1 citation เสมอ, [[rule-engine-verification]]
  มี edge case ใหม่ (#3 แหล่งทุนไม่มีระเบียบ, #10 Project เปลี่ยน FundSource ภายหลังไม่กระทบผลตรวจเก่า)
- **NFR-04**: `db-spec` E-08/E-14 ระบุชัดว่า `ruleVersionId`/`affectedProjectId` เป็น snapshot ของ
  FundSource ที่ Project สังกัด ณ ขณะตรวจ ไม่เปลี่ยนตามแม้ Project ย้าย FundSource ภายหลัง — ทุกไฟล์
  detailed-design ที่เกี่ยวข้อง (รวม [[project-setup]] ใหม่) เขียน AuditLogEntry ครบ
- **NFR-05**: `api-spec` หัวข้อ 2 ระบุตรงๆ ว่าแก้ไขจาก `Fund.ownerUserId` เป็น `Project.ownerUserId`
  แล้ว, [[access-control]] มีตารางบังคับใช้กฎครบทั้ง 21 operation (รวม OP-18–OP-21 ใหม่)

grep ยืนยันว่าการอ้างอิงคำว่า "Fund" ที่เหลืออยู่ในเอกสารทั้งหมดเป็นเพียงข้อความอธิบายประวัติการแก้ไข
(callout "แก้ไข 2026-09-03: เดิม...ซึ่งผิด") ไม่ใช่ field/code จริงที่ยังหลงเหลือ

**ไฟล์ที่แก้ไข**: `docs/02-design/02-technical/nfr-review.md` เท่านั้น — อัปเดตหัวข้อ 0 (เพิ่ม bullet
สรุปรอบตรวจซ้ำ), แถว NFR-03/NFR-04/NFR-05/NFR-07 ในตารางหัวข้อ 1 (ปรับคำอธิบาย/อ้างอิงให้ตรงรหัส
entity ใหม่และเนื้อหาที่ sync แล้ว), หัวข้อ 3 (เพิ่มสรุปผลตรวจซ้ำ), และรายการเอกสารที่เกี่ยวข้องท้ายไฟล์
(เพิ่ม [[project-setup]], ปรับจำนวนไฟล์ detailed-design เป็น 13) — **ไม่แก้ไข** architecture.md/
api-spec.md/db-spec.md/detailed-design/ ใดๆ ทั้งสิ้นตามขอบเขตงานนี้

**งานที่ต้องส่งต่อ**: ไม่มีงานเร่งด่วนจากรอบตรวจนี้ (NFR-03/04/05 ผ่านครบ) — คงคำแนะนำเดิมสำหรับ
NFR-06/NFR-07 (`sync-architecture` เพื่อกำหนดตัวเลขเป้าหมายเชิงปริมาณเมื่อมีข้อมูลจริง ต่อด้วย
`sync-detailed-design`) และคำแนะนำเดิมของรอบก่อนหน้าให้รัน `sync-test-plan` สำหรับฟีเจอร์ที่ 13
(Project Setup) ที่ยังไม่มี test case
