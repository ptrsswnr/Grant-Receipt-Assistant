# BACKLOG.md — Grant Receipt Assistant (Module 2)

> รายการฟีเจอร์/งานที่**ตั้งใจตัดออก**จากขอบเขตสัปดาห์นี้ (ดูเหตุผลเต็มใน [SCOPE.md](SCOPE.md))
> ไม่ใช่สิ่งที่ลืมทำ — บันทึกไว้เพื่อทำต่อใน Sprint ถัดไป

## Sprint 2 — เชื่อมต่อระบบภายนอก/AI จริง

รอ: เอกสารระเบียบทุนวิจัยจริงของมหาวิทยาลัย (สำหรับ Rule Engine) และการตัดสินใจเรื่อง OCR/LLM provider
(ยังไม่ตัดสินใจใน `docs/02-design/02-technical/technology-stack.md`)

- [ ] เชื่อมต่อ OCR service จริง อ่านข้อมูลจากใบเสร็จภาษาไทย (แทนข้อมูล mock) — FR-02
- [ ] Encode ระเบียบทุนวิจัยจริงของมหาวิทยาลัยเข้า Rule Engine (หมวดค่าใช้จ่าย, วงเงินสูงสุดต่อหมวด,
      เอกสารประกอบที่ต้องมี) — FR-04 (2026-09-10: mock checker ใน `new-receipt.js` เปลี่ยนจาก
      hardcode เป็นอ่านจาก `ruleItems` จริงแล้ว — ยังไม่ใช่ Rule Engine จริง แค่ data-driven mock)
- [ ] เชื่อมต่อ LLM API จริง แปลผล Rule Engine เป็นภาษาที่เข้าใจง่าย (แทนคำอธิบาย mock) — FR-05
      (2026-09-10: แนะนำแนวทาง RAG — ให้ LLM อ้างอิงเอกสารระเบียบต้นฉบับที่แนบไว้ตรงๆ ตอนตัดสิน
      แทนการพยายาม parse ระเบียบทั้งหมดเป็น structured data ล่วงหน้า เพราะระเบียบจริงมีเงื่อนไข/
      ข้อยกเว้นแบบ prose เยอะเกินจะ formalize ได้หมด — ดู `docs/05-log/20260910-log.md`)
- [x] Admin นำเข้า/อัปเดตระเบียบทุนวิจัยเข้าสู่ Rule Engine เมื่อระเบียบเปลี่ยน — FR-11 (2026-09-10:
      ทำแล้ว — `app/fund-sources.html` ให้ admin เพิ่ม/ลบแหล่งทุน และแก้ไข `ruleItems`
      (หมวด/ประเภทอัตรา/จำนวนเงิน/หน่วย/เอกสารที่ต้องใช้/หมายเหตุ) ต่อแหล่งทุนแต่ละแหล่งได้เต็มรูปแบบ
      ผ่าน UI ตาราง — ยังไม่มี: บังคับใช้ (enforce) ชนิดเอกสารแนบจริง, ไม่มี version-history UI (แก้
      ในเวอร์ชัน active เดียวเท่านั้น), ไม่ได้ import จากไฟล์ระเบียบต้นฉบับ (PDF/Word) อัตโนมัติ — กรอก
      มือทั้งหมด

## Sprint 2+ — ฟีเจอร์อื่นที่ตัดออกจาก MVP สัปดาห์นี้

- [x] หน้า UI ให้นักวิจัยสร้าง/แก้ไขโครงการวิจัยเอง (Project Setup) — FR-23 (2026-09-10: ทำแล้ว —
      `app/projects.html`/`app/js/projects.js` ให้สร้างโครงการเองได้ ยังไม่มีฟอร์ม**แก้ไข**โครงการที่
      สร้างไปแล้ว มีแค่สร้างใหม่)
- [ ] แก้ไขและอัปโหลดใบเสร็จซ้ำเมื่อไม่ผ่านตรวจ — FR-07
- [x] Admin Dashboard สรุปภาพรวม/รายละเอียดใบเสร็จข้ามทุก user — FR-08 (2026-09-10: ทำแล้วเป็น
      **Admin Dashboard** ข้ามผู้ใช้ทั้งหมด (ไม่ใช่ dashboard ระดับนักวิจัยต่อโครงการอย่างที่ FR-08 เดิม
      เขียนไว้) — ผู้ใช้ขอเปลี่ยนขอบเขตนี้ตรงๆ พร้อมกับกลับคำตัดสินใจ FR-12/NFR-05 (ดู
      `docs/05-log/20260910-log.md`) แสดง stat รวม, แยกตามสถานะ/หมวด/แหล่งทุน, และตารางใบเสร็จ
      ทุกใบพร้อมชื่อ/อีเมลเจ้าของ — Dashboard ระดับนักวิจัยต่อโครงการเดิมตาม FR-08 ยังไม่มี ถ้าต้องการ
      เพิ่มทีหลังได้)
- [ ] แจ้งเตือนใบเสร็จที่มีปัญหายังไม่แก้ไข — FR-09
- [ ] Export รายงานสรุปใบเสร็จที่ผ่านตรวจแล้ว — FR-10
- [ ] ป้องกันการ Export เมื่อยังไม่มีใบเสร็จสถานะผ่าน — FR-21

## Sprint 3 — PDPA Compliance

- [x] แสดง Privacy Notice ก่อนใช้งานครั้งแรก/ก่อนอัปโหลด — FR-13 (2026-09-10: ทำแบบเบา — checkbox
      ยืนยันบน `new-receipt.html` ก่อนอัปโหลดใบเสร็จครั้งแรก บอกว่า Admin เห็นข้อมูลได้เต็มรูปแบบผ่าน
      Admin Dashboard และอาจนำไปใช้ทำรายงาน/ปรับปรุงระบบ — เกิดขึ้นเพราะ FR-12 ถูกกลับคำตัดสินใจ ดู
      `docs/05-log/20260910-log.md` **ยังไม่ใช่ PDPA compliance เต็มรูปแบบ**: ไม่มีหน้า privacy notice
      แยก ไม่มี versioning ของข้อความ consent)
- [x] ขอความยินยอมชัดแจ้งก่อนเก็บ/ประมวลผลข้อมูล — FR-14 (2026-09-10: ทำแบบเบาคู่กับ FR-13 ข้างบน —
      ต้องติ๊ก checkbox ก่อนปุ่ม "อัปโหลดและอ่านข้อมูลด้วย AI" ถึงจะกดได้ครั้งแรก)
- [ ] บันทึกหลักฐานการให้ความยินยอม (Consent Record) — FR-15 (2026-09-10: มีแค่ timestamp เดียว
      `privacyConsentAcceptedAt` บน `users/{uid}` — ไม่ใช่ Consent Record แยกเป็นเอกสารของตัวเอง
      พร้อม audit trail ตามที่ FR-15 ต้องการเต็มรูปแบบ ยังต้องทำต่อ)
- [ ] ถอนความยินยอม + ผลกระทบต่อกระบวนการตรวจ — FR-16
- [ ] ขอเข้าถึง/ขอสำเนาข้อมูลส่วนบุคคลของตนเอง — FR-17
- [ ] ลบ/ขอลบข้อมูลใบเสร็จของตนเองผ่าน UI (Right to Erasure) — FR-18
- [ ] ลบข้อมูลทั้งหมดทันทีเมื่อถอนความยินยอม — FR-20

## Sprint 4 — ขยาย Rule Editor (ตัดออกจากรอบ Admin Dashboard นี้โดยตรง — 2026-09-10)

ผู้ใช้ยืนยันชัดเจนว่ายังไม่ต้องทำ 2 ข้อนี้ตอนสร้าง Admin Dashboard/Rule Editor รอบนี้ — บันทึกไว้ต่อยอด:

- [ ] บังคับใช้ (enforce) ว่าใบเสร็จแต่ละหมวดต้องแนบเอกสารประเภทที่ระบุใน `ruleItem.requiredEvidenceType`
      จริง (ตอนนี้เป็นแค่ field อ้างอิงให้อ่าน ไม่มีการตรวจสอบอัตโนมัติ)
- [ ] ตรวจสอบ/เทียบใบเสร็จกับข้อเสนอโครงการวิจัยต้นฉบับที่อาจารย์ยื่นไว้ตอนขอทุน (เช่น ค่าตอบแทน/
      ค่าจ้าง ที่ไม่มีเพดานระดับแหล่งทุน แต่ขึ้นกับตัวเลขที่ระบุไว้ในข้อเสนอโครงการของแต่ละโครงการเอง)

## UX cleanup (2026-09-10, ไม่ผูกกับ FR รหัสใดรหัสหนึ่งโดยเฉพาะ)

- [x] เอาลิงก์ "🌱 ใส่ข้อมูลตัวอย่าง" ออกจากเมนูบนทุกหน้า (`app/js/nav.js`) และออกจากหน้าแรก/
      `receipts.html`/`new-receipt.html` — ตอนนี้มี UI สร้างโครงการ/แหล่งทุน/อัปโหลดใบเสร็จของจริงแล้ว
      ไม่ควรชี้ผู้ใช้ไปทางข้อมูลตัวอย่างอีก ตัวไฟล์ `app/seed.html` เองยังอยู่ (เข้าตรงผ่าน URL ได้ถ้า
      จำเป็นตอน dev) แค่ไม่มีทางเข้าจาก UI แล้ว
- [x] ลิงก์เมนู "Admin Dashboard"/"แหล่งทุน (Admin)" ไม่แสดงให้ user ทั่วไปเห็นอีกต่อไป (เดิมแสดงทุกคน
      แต่กันสิทธิ์แค่ตอนเข้าเนื้อหาในหน้า) — `app/js/auth-guard.js` เช็ค `isAdmin` จาก Firestore แล้ว
      เรียก `window.showAdminNavLinks()` (จาก `app/js/nav.js`) เพิ่มลิงก์เฉพาะบัญชี admin จริง
- [x] `app/projects.html`: แต่ละการ์ดโครงการมีลิงก์ "ใบเสร็จในโครงการนี้" (`receipts.html?projectId=`)
      และ "อัปโหลดใบเสร็จ" (`new-receipt.html?projectId=` — preselect โครงการให้อัตโนมัติ) —
      `receipts.html` กรองตาม `projectId` ใน URL ได้แล้ว (ไม่กรอง = เห็นของทุกโครงการเหมือนเดิม)

## นอกขอบเขตของโปรเจกต์นี้ (ยืนยันแล้วว่าไม่ทำแม้ใน Sprint ถัดไป)

ดู [[20260816-01-grant-receipt-verification#2.2 ตัดออกจากขอบเขต MVP นี้ (ยืนยันโดยผู้ใช้เมื่อ 2026-08-16 — ดูหัวข้อ 7)]]
ในเอกสาร spec หลัก — รายการเหล่านี้ถูกตัดออกจากขอบเขตทั้งโปรเจกต์ ไม่ใช่แค่เลื่อน sprint:

- Role/action ของ "เจ้าหน้าที่การเงิน" ในระบบ
- Role/workflow ของ "ผู้อนุมัติทุน" ในระบบ
- Deadline tracking รายทุนและการแจ้งเตือนตามกำหนดเวลาส่งหลักฐาน

## อ้างอิง

รายละเอียดเต็มของแต่ละ FR/NFR ดูที่ `docs/01-requirements/backlog.md` และเอกสาร spec ต้นทาง
([[20260816-01-grant-receipt-verification]], [[20260816-02-pdpa-compliance]])
